import React from "react";

function NavBar() {
    return (
        <nav className="border-1 border-red-500">
            <ul>
                <a href="#">Home</a>
                <a href="#">GitHub</a>
                <a href="#">Resume</a>
            </ul>
        </nav>
    );
}

export default NavBar;